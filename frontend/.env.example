# Base path of the API as seen by the browser (dev server proxies /api to VITE_PROXY_TARGET)
VITE_API_URL=/api/v1
# Where the Vite dev server forwards /api requests (backend uvicorn)
VITE_PROXY_TARGET=http://localhost:8000
