/** Compilation/Verification re-exports — the verification screen works the allocation queue. */
export {
  allocationActions,
  allocationsApi,
  optionFetchers,
  verificationsApi,
  type Allocation,
  type Verification,
  type VerificationBrief,
  type VerificationInput,
} from '@/features/allocation/api'
