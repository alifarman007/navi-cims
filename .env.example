# Root-level pointer only. Real settings live in:
#   backend/.env   (copy backend/.env.example)  — DATABASE_URL, SECRET_KEY, SMTP, BNPIMS, seed admin
#   frontend/.env  (copy frontend/.env.example) — VITE_API_URL
